/**
 * The package version, in its own module so both the plugin and the About box
 * can read it without importing each other.
 * @module dsh-tvision/version
 */

/** The published version. Kept in step with `package.json` by the release task. */
export const VERSION = '0.1.0'
